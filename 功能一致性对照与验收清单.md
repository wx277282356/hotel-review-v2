# 功能一致性对照与验收清单

**对照对象**：旧系统 `hotel-review/`（线上正在使用）↔ 新系统 `hotel-review-v2/`（重构中）
**编制日期**：2026-09-12
**基线原则**：旧系统是"标准答案"。凡旧系统有的对外表现，新系统都要能解释为什么不一样——要么补齐，要么由你明确拍板"不再保留"。
**源文件说明**：本 `.md` 是源文件，同名 `.docx` 由它转出。**要改内容请改 `.md` 再重新转**（改 Word 不会同步回 md）。
**代码规模参考**：旧系统 4 个页面 / 约 3200 行（`admin.html` 1436、`index.html` 920、`db.js` 385、`style.css` 416）；新系统 前端约 1100 行（`Admin.vue` 400、`StaffAdmin.vue` 163、`SiteSettingsEditor.vue` 154、`Kiosk.vue` 140）+ 后端约 940 行（`Program.cs` 614）。

---

## 一、怎么用这份清单

1. **第四节到第十二节是"逐项对照表"**，每一行都带一列「一致性验收标准」——这一列就是验收时的判定依据，可以直接照着点、照着截图。
2. **等级说明**（写在每一节表头行下方的说明里，也汇总在第十五节）：
   - 🔴 **P0** = 客人直接看到/操作到的，或店总每天要用的查询与导出。**不对齐就不算交付。**
   - 🟠 **P1** = 统计可视化、运营工具类。功能上要一致，但可以排在 P0 之后。
   - 🟡 **P2** = 装饰性、提示样式、非运行时产物。
   - ⚪ **有意不同** = 新系统架构决定不需要一致，但**要你确认并告知客户**。
   - ❓ **需拍板** = 旧系统自身行为有矛盾/bug，或与你之前的选择冲突，必须你定。
3. **第十四节是"需要你拍板的问题清单"**——这一节请优先看，价格在于：它决定了后面几十项怎么改。
4. **附录 A 是抽样核对法**（用同一批数据在新旧系统跑，逐字段比对），**附录 B 是旧系统已知 bug 清单**——对齐 bug 是没意义的，附录 B 里的每一条都需要你选"照旧保留"还是"按合理行为实现"。

---

## 二、结论摘要（差距盘点）

| 板块 | ✅ 已一致 | 🟡 部分一致 | ❌ 缺失（新系统没有） | ⚪ 有意不同 | ❓ 需拍板 |
|---|---|---|---|---|---|
| 客人点评页（扫码页） | 3 | 3 | 8 | 1 | 3 |
| 后台 · 信息架构与顶栏 | 0 | 1 | 3 | 1 | 0 |
| 后台 · 数据看板 | 1 | 0 | 4 | 0 | 1 |
| 后台 · 统计报表 | 0 | 1 | 5 | 0 | 1 |
| 后台 · 评价记录（查询） | 2 | 2 | 6 | 0 | 1 |
| 后台 · 系统设置 | 2 | 2 | 5 | 1 | 2 |
| 后台 · 账号与原因管理 | 3 | 2 | 3 | 1 | 0 |
| 二维码下载页 | 2 | 0 | 6 | 0 | 0 |
| 数据模型 / 统计口径 | 4 | 3 | 3 | 1 | 3 |
| 导出 / 导入 / 打印 | 1 | 1 | 2 | 1 | 2 |
| 视觉与文案基线 | 1 | 3 | 4 | 0 | 1 |
| 角色与权限 | 2 | 1 | 0 | 1 | 2 |
| **合计（约）** | **21** | **19** | **49** | **8** | **18** |

**一句话结论**：新系统目前是一个**"能跑通、能用"的最小闭环**——客人能提交、数据集中进数据库、后台能看总数和明细、能导出 CSV。但**离"与旧系统完全一致"还有明显距离**，差距集中在三块：

1. **客人页的完整体验**（双语、语音播报、时钟、独立差评屏与感谢页、倒计时、装饰）——旧版 920 行，新版 140 行，属于"重新设计了一版"而非"复刻"。
2. **后台的查询与统计能力**（日期范围筛选、5 个筛选维度、分页、周期报表、4 张图表、看板"今日"数据）——**新系统完全没有日期查询，后端把所有评价一次性全量返回**，数据量一上来就会变慢且没法按时间段查。
3. **导出能力**（旧系统是 xlsx 多工作表带标题行与列宽，新系统是 CSV 单表）。

---

## 三、验收总则（所有项目通用）

| 编号 | 准则 | 具体判定方式 |
|---|---|---|
| G1 | **对照法** | 同一台设备、同一批数据，左侧开旧系统、右侧开新系统，逐屏逐项比对；不一致处截图留存 |
| G2 | **数据一致法** | 用同一份评价数据分别在新旧系统查询，**逐字段逐行**比对（见附录 A），条数、字段值、排序必须完全一致 |
| G3 | **文案一致** | 面向用户的按钮/标题/提示/空态文字，与旧系统**逐字一致**（含中英文、含 emoji、含标点） |
| G4 | **行为一致** | 同一操作序列在新旧系统的结果一致（例如：差评不选原因点提交 → 旧系统弹确认框） |
| G5 | **口径一致** | 统计数字必须能对上：分子分母、四舍五入规则、空值归类、排序规则（详见第八节） |
| G6 | **不回归** | 对齐过程不得破坏已有能力：评价不可删除、客人免登录提交、房间号一房一码、跨设备汇总、限流与入参校验 |
| G7 | **差异须登记** | 任何一处"故意不同"都必须写进第十三节并经你确认，不允许静默偏离 |

---

## 四、页面与入口对照

| 编号 | 页面 | 旧系统入口 | 新系统入口 | 差异 | 一致性验收标准 | 等级 |
|---|---|---|---|---|---|---|
| P-1 | 客人点评页 | `/hotel-review/index.html`，支持 `?room=<房号>` | `/hotel-review-v2/`，支持 `?room=<房号>` | 路径不同（域名子路径变化，需重做二维码） | 扫码后能打开对应房间的点评页；URL 形态与旧系统同为"一个房间一个链接" | ⚪ |
| P-2 | 管理后台 | `/hotel-review/admin.html` | `/hotel-review-v2/?admin=1` | 入参不同 | 从客人页/书签能进后台；后台地址可被酒店人员记住 | 🟠 |
| P-3 | 二维码下载页 | `/hotel-review/qrcodes/index.html` | `/hotel-review-v2/qrcodes/` | 功能缺 6 项（见第十节） | 见 Q1–Q11 | 🟠 |
| P-4 | 台卡设计稿（印刷用） | `台卡设计稿.html`（静态设计稿） | **无** | 新系统没有 | ❓需你确认是否还需要这份设计稿（属印刷物料，非运行时功能） | 🟡 |
| P-5 | 房间清单 | `rooms.txt` + `qrcodes/rooms.json`（221 间） | `frontend/scripts/rooms.txt`（221 间） | 清单一致 ✓ | 房间号集合必须完全相同（含 `1801A/1801B/1802A/1802B` 这类含字母房号） | 🔴 |

---

## 五、客人点评页（扫码页）逐项对照

> 旧版结构是**一个页面 3 个"屏幕"**：① 主页（好评/差评）→ ② 差评原因屏 → ③ 感谢屏，外加语音与时钟。新版是**单屏表单**（选类型 + 选原因 + 点提交），感谢用一个覆盖层。

### 5.1 布局与视觉

| 编号 | 项目 | 旧系统（基线） | 新系统（现状） | 差异 | 一致性验收标准 | 等级 |
|---|---|---|---|---|---|---|
| K-1 | 页面整体结构 | 3 个全屏 screen 切换，切换动画 `screenIn .5s cubic-bezier(.4,0,.2,1)` | 单屏竖向排列，无屏幕切换 | ❌ 结构不同 | 客人操作路径与旧版一致：好评/差评入口在第一屏；差评原因在独立屏；感谢页为独立全屏 | 🔴 |
| K-2 | 品牌区 | 圆形金底 LOGO 容器（`assets/logo-icon.png`）+ 中文名 + 英文名 + 提示语；LOGO 有 `logoPulse 3s` 呼吸动画；自定义 LOGO 时去掉金底与混合模式 | 72×72 圆角方形金渐变底 LOGO + 中文名 + 英文名 + 提示语；无动画 | 🟡 视觉不同 | LOGO 呈圆形金色底、有呼吸动画；上传自定义 LOGO 后自动去掉金底（不出现"金框里套一张图"的别扭效果） | 🟠 |
| K-3 | 主标题 | 「服务点评」/「SERVICE REVIEW」（中文用 `STSong, SimSun, serif`） | **无** | ❌ 缺 | 页面有主标题「服务点评」+ 英文副标「SERVICE REVIEW」，字体与旧版一致（中文衬线） | 🔴 |
| K-4 | 装饰元素 | 四角金色角框（`.corner-ornament` ×4）+ 金色"线-菱形-线"分隔饰 | **无** | ❌ 缺 | 四角金框与分隔饰还原；位置、颜色与旧版一致（金色 `#c9a84c`、浅金 `#e8d5a3`） | 🟡 |
| K-5 | 提示语 | 中文 `voicePrompt` + 英文 `voicePromptEn` 两行同屏 | 仅一行中文 `guestPrompt`（无英文） | 🟡 缺英文 | 提示语支持**中英两行**同时显示，内容可在后台分别配置 | 🔴 |
| K-6 | 房间号展示 | 有 `?room=` 时显示「🛏 当前房间 ROOM 802」；**无参数时整行留空** | 始终显示「房间号」标签；有参数显示锁定值，无参数显示输入框 | 🟡 样式不同（新增加了手填） | 有 `?room=` 时显示「🛏 当前房间 ROOM <房号>」形态且不可修改；无参数时的表现按第十四节拍板 | 🔴 |
| K-7 | 移动端适配 | ≤640px 一套适配（品牌区变横排、按钮横排、**隐藏右下角管理入口**）；≤360px 再微调；用 `100dvh` | 仅简单自适应，无分级断点 | 🟡 未对齐 | iOS/安卓平板与手机的横竖屏下不出现滚动条错位、按钮不被裁切；≤640px 时管理入口按旧版隐藏 | 🟠 |
| K-8 | 页面标签页标题 | `服务点评 \| 城市酒店` | `<酒店名> · 点评系统` | 🟡 文案不同 | 标签页标题格式与旧版一致（含酒店名） | 🟡 |

### 5.2 交互行为（核心）

| 编号 | 项目 | 旧系统（基线） | 新系统（现状） | 差异 | 一致性验收标准 | 等级 |
|---|---|---|---|---|---|---|
| K-9 | 好评提交路径 | **点一下「😊 好评」直接提交**（无原因选择，`reasons: []`，无二次确认） | 需先切到"好评"、可能选好评原因、再点「提交评价」 | ❌ 多 1–2 步 | 严格一致 = 好评一键提交。**但**你此前已选择"好评选项可配置"，两者冲突 → 见第十四节 Q1 | ❓ |
| K-10 | 差评提交路径 | 点「😞 差评」→ 切到独立屏选原因 → 点「提交 Submit」（可点「← 返回 Back」退回） | 同一屏选原因 → 点「提交评价」 | ❌ 少一个"返回"出口 | 差评屏为独立页面/屏幕，含「← 返回 Back」与「提交 Submit」两个按钮 | 🔴 |
| K-11 | 好评/差评按钮形态 | 两个大按钮，默认**都未选中**（不存在"当前选中类型"概念） | 两个按钮有选中态（默认选中"好评"） | ❌ 语义不同 | 按钮为"动作按钮"而非"选项按钮"：点下去即执行，不存在默认选中态 | 🔴 |
| K-12 | 差评原因展示 | chip 上**中文 + 小号英文**两行（如「服务差 / Poor Service」） | 仅中文一行 | 🟡 缺英文 | 原因按钮支持中英双行显示，英文可配置 | 🟠 |
| K-13 | 差评未选原因的确认 | 一个原因都没选就点提交 → 弹确认框「您未选择任何原因，确认提交吗？\nNo reason selected. Submit anyway?」，取消则中止 | **无确认，直接提交空原因** | ❌ 缺确认 | 差评未选原因点提交时弹出同一文案的确认框；取消后停留在原因屏且不产生数据 | 🔴 |
| K-14 | 原因多选 | 可多选，按点击顺序收集 | 可多选，按点击顺序收集 | ✅ 一致 | 多选、点击取消选中、顺序与点击顺序一致 | 🔴 |
| K-15 | 提交按钮 | 在差评屏内（「提交 Submit」）；好评无提交按钮 | 独立「提交评价」按钮在原因区下方 | 🟡 位置不同 | 与 K-9/K-10 拍板结果一致 | ❓ |
| K-16 | 提交后反馈 | 切到独立感谢屏：好评=金色五角星 SVG + 「感谢您的好评！」+ 配置文案；差评=机器人头像动画（`bounceIn .6s` + 眨眼 `blink 3s`）+「感谢您的宝贵意见！」+ 配置文案；均含**英文版** | 全屏覆盖层：✅ emoji + 一行配置文案；**无图形、无英文** | ❌ 缺图形与英文 | 感谢屏与旧版一致：好评星形、差评机器人；中英双语；配置文案原样显示 | 🔴 |
| K-17 | 自动返回 | 倒计时「X 秒后自动返回 / Returning in Xs...」，秒数取配置 `autoReturn`（默认 4，后台可设 2–10），归零后回到主页并重置状态 | 固定 3000ms 后关闭覆盖层，**不可配置、无文案** | ❌ 不可配置且无文案 | 倒计时文案与旧版一致；秒数由后台配置控制，改配置后客人页刷新即生效 | 🔴 |
| K-18 | 语言与语音 | 中英文同屏显示（标题/按钮/提示/原因/感谢语）；语音播报：按钮「语音提醒 Voice Prompt」手动播报 + **进页 30 秒首次自动播报、之后每 60 秒一次**，文案用 `voicePrompt`，`rate 0.9`，进入差评屏暂停、回主页重排 | **完全没有**：无语音按钮、无自动播报、无英文 | ❌ 缺 | 语音按钮与自动播报节奏（30s/60s）与旧版一致；文案可后台配置；播报期间不重复叠加 | 🔴 |
| K-19 | 时钟 | 右上/下方显示当前时间 `YYYY.MM.DD  HH:mm`，每秒刷新 | **无** | ❌ 缺 | 显示实时时钟，格式与旧版一致（点分隔日期、时分间双空格） | 🟠 |
| K-20 | 管理入口 | 右下角「⚙ 管理」，`window.open('admin.html','_blank')` 新标签打开后台；≤640px 隐藏 | 后台入口在顶部导航（仅非客房模式可见）；客房扫码页无入口 | 🟡 入口位置不同 | 前台平板（无 `?room=`）能进后台；客人扫码页（有 `?room=`）不出现后台入口 | 🟠 |
| K-21 | 防重复提交 | **无**：连点好评按钮会写入多条记录 | 有 `submitting` 锁，提交中按钮禁用 | ⚪ 新版更优 | 保留新系统的防重复（建议）；若要求严格一致则去掉 → 见第十四节 Q2 | ❓ |
| K-22 | 前台平板常态页（无 `?room=`）行为 | 房间号留空；提交的评价房间号为 `''` | 提供房间号输入框（可不填） | 🟡 新增加能力 | 按第十四节 Q3 拍板：保留手填（推荐）or 完全照旧留空 | ❓ |

---

## 六、后台逐页对照

### 6.1 信息架构与顶栏

| 编号 | 项目 | 旧系统（基线） | 新系统（现状） | 差异 | 一致性验收标准 | 等级 |
|---|---|---|---|---|---|---|
| B-1 | 导航结构 | 左侧固定侧边栏（240px，≤768px 变 200px，≤640px 抽屉）+ 右侧内容区，**5 个菜单页**：数据看板 / 统计报表 / 评价记录 / 系统设置 / 账号管理 | **单页纵向滚动**，所有模块堆在一页，无菜单、无页面切换 | ❌ 结构完全不同 | 有侧边栏 5 项菜单，点击切换对应内容区；当前项高亮；菜单顺序与旧版一致 | 🟠 |
| B-2 | 顶栏 | 页面标题 + `☰`（移动端按钮）+「📱 前台展示」+ 时钟 `YYYY/MM/DD HH:mm`（每秒刷新） | 无顶栏（仅"<酒店名> · 点评后台"标题行） | ❌ 缺 | 顶栏含当前页标题、「📱 前台展示」入口、实时时钟，格式与旧版一致 | 🟡 |
| B-3 | 侧边栏底部用户区 | 头像（姓名首字）+ 姓名 + 角色（`店总 Manager` / `前台 Front Desk`）+ 退出按钮 | 顶部"身份栏"：姓名 + 角色徽标（管理员/查看者）+ 修改密码 + 退出登录 | 🟡 位置与文案不同 | 显示头像（姓名首字）、姓名、角色中文；含退出按钮 | 🟠 |
| B-4 | 移动端侧边栏 | `☰` 按钮内联 `display:none` 且无 JS 使其可见 → **≤640px 侧边栏无法打开**（旧版 bug） | 单页布局天然可用 | ⚪ 新版更优 | 移动端能进所有功能页。**不要照抄旧版的失效 ☰** | ⚪ |
| B-5 | 后台标签页标题 | `管理后台 \| 城市酒店服务点评` | `<酒店名> · 点评后台`，标签页为 `<酒店名> · 点评系统` | 🟡 文案不同 | 标题含酒店名，格式与旧版一致 | 🟡 |

### 6.2 数据看板

| 编号 | 项目 | 旧系统（基线） | 新系统（现状） | 差异 | 一致性验收标准 | 等级 |
|---|---|---|---|---|---|---|
| B-6 | 顶部统计卡片 | **5 张**：今日好评 / 今日差评 / 今日合计 / 累计总评价 / 累计好评率 | **4 张**：总评价 / 好评 / 差评 / 好评率 | ❌ 缺"今日"三项 | 5 张卡齐全，标签文案一致；"今日"按**本地日期**0 点起算 | 🔴 |
| B-7 | 近 7 天趋势图 | 分组柱状图（好评 `rgba(74,140,94,.7)` / 差评 `rgba(140,61,61,.7)`），x 轴最近 7 天、`MM-DD`，y 轴整数、从 0 起 | **无图表** | ❌ 缺 | 柱状图存在，7 天标签格式 `MM-DD`，颜色与旧版一致，柱数=7 | 🟠 |
| B-8 | 好差评占比图 | 环形图（doughnut，`cutout 65%`），图例在下，**全量**数据 | **无** | ❌ 缺 | 环形图存在，中心留空比例与图例位置一致，取全量数据 | 🟠 |
| B-9 | 差评原因排行 | 列表：序号 + 原因名 + 进度条（宽度=该项次数/最大值）+ 次数；**全量**、次数降序；空态「暂无差评数据」 | **无** | ❌ 缺 | 排行存在、按次数降序、进度条比例正确、空态文案一致 | 🟠 |
| B-10 | 差评房间预警 | 表：房间号 / 差评数 / 好评数 / 合计 / 差评率；数据=**近 30 天**、仅 `negative>0`、取前 10；差评数 ≥3 的行浅红底 `rgba(140,61,61,.12)`；右侧固定文案「客房部重点关注」；空态「近30天无差评记录 🎉」 | **无** | ❌ 缺 | 该表存在；取近 30 天、Top 10；≥3 行有浅红底；文案与空态一致 | 🟠 |
| B-11 | 看板"今日/近7天"日期口径 | 用 `toISOString()` 取日期（**UTC 日**），东八区 0–8 点会错一天（旧版 bug） | 无此功能 | ❓ 旧版 bug | 新实现按**本地日期**口径（推荐），或照旧保留 UTC → 见第十四节 Q4 | ❓ |

### 6.3 统计报表

| 编号 | 项目 | 旧系统（基线） | 新系统（现状） | 差异 | 一致性验收标准 | 等级 |
|---|---|---|---|---|---|---|
| B-12 | 统计周期切换 | 「按天 / 按周 / 按月」三个 tab，默认**按天**，切换后图表与汇总表随之重算 | **无** | ❌ 缺 | 三个周期 tab 存在、默认按天；切换后数据按对应维度重新聚合 | 🔴 |
| B-13 | 日期范围筛选 | 开始/结束日期，**进入页面自动填「今天−29 天 ~ 今天」**；已有值时不覆盖 | **无** | ❌ 缺 | 日期范围可筛；初始默认值与旧版一致（近 30 天） | 🔴 |
| B-14 | 统计图表 | 按周期维度的分组柱状图（好评/差评） | **无** | ❌ 缺 | 图表随周期与日期范围变化 | 🟠 |
| B-15 | 汇总数据表 | 列：周期 / 好评数 / 差评数 / 合计 / 好评率；按周期升序；标题右侧「合计：好评 X / 差评 Y / 好评率 Z%」 | **无** | ❌ 缺 | 列顺序与文案一致、按周期升序、右侧汇总行文案一致 | 🔴 |
| B-16 | 按工号统计 | 列：工号 / **姓名** / 好评数 / 差评数 / 合计 / 好评率；按 `total` 降序；空态「暂无数据（评价记录中暂无工号信息）」 | 列：工号 / 总数 / 好评 / 差评（**无姓名、无好评率**）；按 `total` 降序 ✓ | 🟡 缺 2 列 | 6 列齐全（含姓名与好评率）；排序与空态文案一致 | 🔴 |
| B-17 | 按房间统计 | 列：房间号 / 好评数 / 差评数 / 合计 / 好评率；**好评率 <60% 显示红色，否则绿色**；空态「暂无数据」 | 列：房间 / 总数 / 好评 / 差评（**无好评率、无颜色规则**） | 🟡 缺 1 列 + 颜色 | 5 列齐全；好评率 <60% 变红、否则绿 | 🔴 |
| B-18 | 报表页筛选控件 | 有 5 个控件（类型/差评原因/操作工号/房间号/日期），**但类型、原因、工号三个读了值却没生效**（旧版 bug） | 无 | ❓ 旧版 bug | 见第十四节 Q5：要"真的生效"还是要"照旧不生效" | ❓ |

### 6.4 评价记录（数据查询主战场）

| 编号 | 项目 | 旧系统（基线） | 新系统（现状） | 差异 | 一致性验收标准 | 等级 |
|---|---|---|---|---|---|---|
| B-19 | 日期范围筛选 | 有；**默认「今天−29 天 ~ 今天」**；开始=本地 0 点，结束=本地 23:59:59.999 | **无**（后端 `GET /api/reviews` 返回全量） | ❌ 缺核心能力 | 可按日期区间筛选；边界口径=本地日 0 点至 23:59:59.999；默认值与旧版一致 | 🔴 |
| B-20 | 类型筛选 | 有（全部/好评/差评），精确匹配 | 有，但仅**前端**对已加载的全量数据过滤 | 🟡 实现方式不同 | 结果集合与旧版一致（同一筛选条件下条数相同） | 🔴 |
| B-21 | 差评原因筛选 | 有，`reasons.includes(原因)` 精确匹配 | **无** | ❌ 缺 | 可按单个原因筛选；多选原因的评价只要包含该原因即命中 | 🔴 |
| B-22 | 操作工号筛选 | 有，`staffUsername` 精确匹配 | **无** | ❌ 缺 | 可按工号精确筛选 | 🔴 |
| B-23 | 房间号筛选 | 有，大小写不敏感精确匹配 | **无** | ❌ 缺 | 可按房间号筛选，`802` 与 `802` 大小写差异不影响命中 | 🔴 |
| B-24 | 分页 | 每页 **20** 条 + 页码按钮（仅 >1 页时显示）；筛选条件变化时**不重置回第 1 页**（只做越界归 1） | **无分页**，一次性渲染全部 | ❌ 缺 | 20 条/页、页码可点、越界归 1；大数据量下页面不卡 | 🔴 |
| B-25 | 明细表列与文案 | 5 列：**时间 / 房间号 / 评价 / 差评原因 / 操作工号**；「评价」列显示 `😊 好评` / `😞 差评` 徽标；工号列 = 工号换行显示姓名 | 5 列：时间 / 类型 / 房间 / 原因 / 员工；徽标无 emoji；工号列**无姓名** | 🟡 列名与内容不同 | 列标题与顺序一致；评价列带 emoji 徽标；工号列显示"工号 + 换行姓名" | 🔴 |
| B-26 | 时间显示格式 | `YYYY-MM-DD HH:mm`（无秒） | `new Date().toLocaleString()`（随系统区域变化，如 `2026/9/12 23:31:00`） | 🟡 格式不同 | 表格内统一 `YYYY-MM-DD HH:mm` | 🔴 |
| B-27 | 计数文案 | 标题右侧「共 N 条记录」 | **无** | ❌ 缺 | 显示「共 N 条记录」，N 随筛选变化 | 🟠 |
| B-28 | 空值显示 | 房间号/原因/工号为空显示灰色 `-` | 空值显示 `-` ✓ | ✅ 一致 | 空值一律显示 `-`，且为灰色 | 🔴 |
| B-29 | 空态文案 | 记录页「暂无数据」；报表-按工号「暂无数据（评价记录中暂无工号信息）」；报表-按房间「暂无数据」 | 「暂无数据」 | 🟡 部分缺 | 各页空态文案与旧版逐字一致 | 🟠 |
| B-30 | 排序 | 明细按写入顺序（≈最新在前）；按房间 `negative desc, total desc` ✓；按工号 `total desc` ✓ | 明细 `CreatedAt desc` ✓；按房间 `negative desc, total desc` ✓；按工号 `total desc` ✓ | ✅ 一致 | 三个排序规则与旧版一致 | 🔴 |
| B-31 | 数据量性能 | 纯本地数组，无网络 | **全量加载 + 全量渲染**，无分页无筛选；评价上万条时会明显变慢 | ❌ 架构性风险 | 1 万条评价下：明细页首屏 ≤3 秒、筛选后 ≤1 秒 | 🔴 |

### 6.5 系统设置

| 编号 | 项目 | 旧系统（基线） | 新系统（现状） | 差异 | 一致性验收标准 | 等级 |
|---|---|---|---|---|---|---|
| B-32 | 设置分区 | 5 区：🏨 酒店基础信息 / 🔊 语音提醒设置 / 💬 感谢语设置 / 📡 捷信达 PMS 接口（禁用） / ☁️ 数据聚合后端 | 1 区「⚙️ 站点设置」 | ❌ 结构精简 | 分区结构与标题文案与旧版一致（PMS/聚合后端两区按第十三节处理） | 🟠 |
| B-33 | 酒店基础信息 | 酒店名称中文、Hotel Name English、**LOGO 上传**（存 base64，实时预览） | 酒店名称、英文名 ✓；LOGO 上传在**单独区块**，存服务端文件 | 🟡 LOGO 位置与存储不同 | 两个名称可改；LOGO 可上传并立即预览，客人页与后台同步生效 | 🔴 |
| B-34 | 语音提醒文案 | `voicePrompt`（中）+ `voicePromptEn`（英），textarea | **无** | ❌ 缺 | 两个语音文案可分别配置 | 🔴 |
| B-35 | 感谢语 | 4 条：好评中/英 + 差评中/英 | 2 条中文（好评/差评） | 🟡 缺英文 | 4 条齐全，中英分别配置 | 🔴 |
| B-36 | 自动返回秒数 | `autoReturn`，number，min 2 / max 10，默认 4 | **无** | ❌ 缺 | 可配置 2–10 秒；客人页倒计时使用该值 | 🔴 |
| B-37 | 差评原因管理 | 位置：账号管理页内「📋 差评原因管理」；每行=启用开关 + 中文名 + **英文名** + 编辑 + 删除；底部「＋ 添加差评原因」；删除需确认「确认删除该差评原因？」；中文必填（缺则提示「⚠ 请填写中文名称」） | 「站点设置」里两个 textarea，**每行一个，整段覆盖保存** | ❌ 交互方式不同 | 每条原因可单独新增/编辑/删除/启用停用，含英文名与启用开关 → 见第十四节 Q6（是否接受 textarea 简化） | 🔴 |
| B-38 | 默认原因标签集 | 默认 8 条差评原因：服务差/Poor Service、卫生差/Poor Hygiene、态度差/Poor Attitude、等待太久/Long Wait、响应不及时/Slow Response、设施老旧/Old Facilities、空调/热水问题/AC · Hot Water Issue、网络差/Poor Wi-Fi；**旧系统好评无原因** | 默认 6 条好评（服务态度好/房间干净/设施完善/位置方便/性价比高/早餐丰富）+ 6 条差评（服务态度差/房间不干净/设施故障/噪音大/网络差/其他） | ❌ **标签集完全不同** | 默认差评原因与旧版一致（同一套标签）；好评原因按第十四节 Q1 决定 | 🔴 |
| B-39 | PMS 预留区 | 3 个 disabled 字段 + 徽标「预留接口 · 待对接」+ 说明文字 | 无 | ⚪ 未实现功能 | 按第十三节/第十四节 Q7 决定是否保留占位展示 | ⚪ |
| B-40 | 数据聚合后端设置区 | 开关 + 后端地址 + 管理员令牌（旧系统默认关闭、从未启用） | 无（新系统已改为集中存储，不需要） | ⚪ 架构决定 | 不保留；但需在交付文档中说明"跨设备汇总已内置" | ⚪ |
| B-41 | 保存反馈 | 成功 toast「✅ 设置已保存」；LOGO 上传 toast「✅ LOGO 已更新」 | 内联文字「✅ 已保存，客人页刷新后生效」 | 🟡 形式不同 | 保存后有明确成功/失败提示，文案与旧版一致 | 🟠 |
| B-42 | 配置校验 | 无（旧系统不校验长度/重复） | 有：名称≤50、提示语≤60、选项≤20 项且每项≤20 字；trim/去空/去重 | ⚪ 新版更优 | 保留校验；但校验失败提示应为中文且指出具体字段 | ⚪ |

### 6.6 登录、账号与原因管理

| 编号 | 项目 | 旧系统（基线） | 新系统（现状） | 差异 | 一致性验收标准 | 等级 |
|---|---|---|---|---|---|---|
| B-43 | 登录页 | LOGO + 酒店名 + 副标「SERVICE REVIEW · MANAGEMENT SYSTEM」+ 账号/密码 + 错误提示「账号或密码错误 / Incorrect username or password」+ 按钮「登 录　LOGIN」+ **默认账号提示「默认账号：admin / staff ｜ 密码：admin123 / staff123」**；账号框回车→聚焦密码、密码框回车→登录 | 标题「请登录后台」+ 用户名/密码（回车即登录）+「登 录」+「用主令牌登录（管理员后门）」；**无默认账号提示** | 🟡 文案不同；默认账号提示已移除 | 副标、错误文案、回车行为与旧版一致。默认账号提示**不建议恢复**（安全）→ 见第十四节 Q8 | 🔴 |
| B-44 | 角色模型 | `manager`（店总 Manager，完整权限）/ `staff`（前台 Front Desk，查看+**无导出**） | `admin`（管理员：全部）/ `viewer`（查看者：看统计明细+导出） | ❌ **权限不同** | 见第十四节 Q9：`viewer` 是否允许导出？若要一致则禁止 | ❓ |
| B-45 | 菜单可见性 | `staff` 看不到「系统设置」「账号管理」 | `viewer` 看不到 LOGO / 站点设置 / 账号管理 | ✅ 逻辑一致 | 非管理员看不到配置类模块 | 🔴 |
| B-46 | 账号字段 | 姓名 Name / 用户名 Username / 密码 / 权限 Role（下拉两项） | 登录名 / 姓名 / 初始密码 / 角色（查看者·管理员） | 🟡 文案与命名不同 | 字段齐全；角色下拉文案与旧版一致（店总 Manager / 前台 Front Desk） | 🟠 |
| B-47 | 新增/编辑账号 | 弹窗；**密码栏 placeholder「留空则不修改密码」**（编辑时不改密）；姓名+用户名必填；新增时密码必填；提示「⚠ 请填写姓名和用户名」「⚠ 新账号请设置密码」 | 新增用一行内联表单；改密用 `prompt` 弹「重置密码」 | 🟡 交互方式不同 | 新增与编辑分开；编辑时留空密码=不改密；必填校验文案与旧版一致 | 🟠 |
| B-48 | 当前账号保护 | 当前登录账号那一行显示「当前账号」文本、**无删除按钮** | 允许删除任意账号（后端限制：不能删自己、不能降级最后一个管理员） | 🟡 前端未体现 | 当前账号行不显示删除按钮（与旧版一致） | 🔴 |
| B-49 | 账号启停 | **无此概念**（只有删除） | 有 `isActive` 停用/启用，停用后立即踢掉会话 | ⚪ 新增加能力 | 保留（有用）；需在交付文档中说明 | ⚪ |
| B-50 | 自助改密 | **无** | 有（原密码+新密码，≥6 位） | ⚪ 新增加能力 | 保留 | ⚪ |
| B-51 | 删除确认文案 | 「确认删除该账号？」 | 「确定删除账号「XXX」？\n\n注意：只删登录账号，他名下的评价数据不受影响。」 | 🟡 文案不同 | 无害差异，可保留新版（信息更全） | 🟡 |
| B-52 | 操作反馈 | 底部居中 toast 胶囊，显示 2200ms | 内联文字（页面上） | 🟡 形式不同 | 按第十三节统一为 toast 或内联，但必须"操作后有明确反馈" | 🟡 |

---

## 七、数据查询逻辑对照（重点）

### 7.1 查询能力矩阵

| 查询维度 | 旧系统（基线） | 新系统（现状） | 一致性验收标准 | 等级 |
|---|---|---|---|---|
| 日期范围 | ✅ 有（默认近 30 天） | ❌ 无 | 传入起止日期后，返回值只含区间内评价；边界=本地 0 点~23:59:59.999 | 🔴 |
| 类型 | ✅ 精确 `positive`/`negative` | 🟡 仅前端过滤 | 同一筛选下条数一致 | 🔴 |
| 差评原因 | ✅ `reasons` 数组包含该文本 | ❌ 无 | 命中"包含该原因"的记录 | 🔴 |
| 操作工号 | ✅ `staffUsername` 精确 | ❌ 无 | 精确命中；空归「未记录」 | 🔴 |
| 房间号 | ✅ 大小写不敏感精确 | ❌ 无 | `1801a` 能命中 `1801A` | 🔴 |
| 多条件组合 | ✅ 全部是"与"关系 | 仅类型 | 多条件同时生效（AND） | 🔴 |
| 分页 | ✅ 20 条/页 | ❌ 全量 | 分页正确；切换筛选不丢结果 | 🔴 |
| 全文/关键词 | ❌ 旧系统也没有 | ❌ 无 | 不需要 | ⚪ |

### 7.2 查询口径细节

| 编号 | 项目 | 旧系统（基线） | 新系统（现状） | 一致性验收标准 | 等级 |
|---|---|---|---|---|---|
| D-1 | 日期边界 | `startDate` → 本地 0 点；`endDate` → 本地 23:59:59.999 | 无 | 选"9月12日"必须能查到当天 00:00 与 23:59 的记录（含边界） | 🔴 |
| D-2 | 时区 | 存储 UTC ISO 字符串；查询时转本地边界 | 存储 `timestamptz`（UTC）；展示时本地化 | 跨时区/跨机器查询结果一致；东八区 0–8 点不缺当天数据 | 🔴 |
| D-3 | 空值归类 | 房间空 → 归「未记录」；工号空 → 归「未记录」，姓名空显示「未知」 | 房间/工号空 → 归「未记录」✓；无意无姓名概念 | 聚合表里出现「未记录」行，且计数正确 | 🔴 |
| D-4 | 排序 | 明细=写入顺序；按房间=`negative desc, total desc`；按工号=`total desc` | 明细=`CreatedAt desc`；后两者一致 ✓ | 三个排序逐项比对一致 | 🔴 |
| D-5 | 好评率精度 | 屏幕：整数 `Math.round(pos/total*100)`（33%）；导出：1 位小数 `toFixed(1)`（33.3%） | API 返回 1 位小数（33.3%），界面直接显示 | 明确统一口径（建议屏幕与导出**都用 1 位小数**，并在文档里写死）→ 见第十四节 Q10 | 🔴 |
| D-6 | 周期聚合 | 天 `YYYY-MM-DD` / 周 `YYYY-Www` / 月 `YYYY-MM`；**周的起算口径混用（周日起算 + ISO 周号）** | 无 | 周期键格式一致；周口径按第十四节 Q11 定 | 🔴 |
| D-7 | 数据量影响 | 本地数组 | 全量返回 + 全量渲染 | 1 万条下首屏 ≤3 秒 | 🔴 |

---

## 八、统计口径对照

| 编号 | 指标 | 旧系统（基线） | 新系统（现状） | 一致性验收标准 | 等级 |
|---|---|---|---|---|---|
| S-1 | 总数 | 全部记录数 | `total` | 同一数据集下数字相同 | 🔴 |
| S-2 | 好评/差评数 | `type==='positive'` 与非 positive | 同 ✓ | 相同 | 🔴 |
| S-3 | 累计好评率 | `round(pos/total*100)`，空数据为 0 | `Math.Round(pos/total*100, 1)`，空数据为 0 ✓ | 精度统一（见 D-5） | 🔴 |
| S-4 | 今日指标 | 今日好评/差评/合计（UTC 日，有 bug） | 无 | 新增今日指标；按本地日（见 Q4） | 🔴 |
| S-5 | 近 7 天趋势 | 按天分组，`MM-DD` 标签 | 无 | 7 天数据与旧版逐日比对一致 | 🟠 |
| S-6 | 差评原因排行 | 全量、按原因计数降序 | 无 | 每条原因的次数与旧版一致 | 🟠 |
| S-7 | 按房间统计 | 全量（报表页受日期范围）；`negative desc, total desc` | 全量、排序 ✓ | 同一数据集下每行 3 个数字相同 | 🔴 |
| S-8 | 按工号统计 | 受日期范围；`total desc`；key 空→「未记录」 | 全量、排序 ✓；**无姓名** | 数字相同；需补姓名列 | 🔴 |
| S-9 | 按房间差评率 | 报表页 `round(negative/total*100)%`，<60% 红 | 无 | 补该列与颜色规则 | 🔴 |
| S-10 | 时间范围筛选后统计 | 各统计函数都接受起止日期 | 全部统计**不接受任何日期参数** | 所有统计都能被日期范围约束 | 🔴 |

---

## 九、数据展示与接口返回结果对照

### 9.1 评价记录字段

| 编号 | 字段 | 旧系统 | 新系统 | 差异 | 一致性验收标准 | 等级 |
|---|---|---|---|---|---|---|
| F-1 | `id` | number（`Date.now()` 毫秒） | Guid 字符串 | 🟡 类型不同 | 前端不依赖 id 形态；迁移时旧 id 不参与去重（用指纹去重） | 🟡 |
| F-2 | `createdAt` | ISO UTC 字符串，如 `2026-09-12T06:30:00.000Z` | `DateTime.UtcNow`（JSON 为 ISO） | ✅ 语义一致 | 迁移后**原始提交时间逐条保持**（已在迁移脚本中实现） | 🔴 |
| F-3 | `type` | `positive` / `negative` | 同 ✓ | ✅ | 取值完全相同 | 🔴 |
| F-4 | `reasons` | string[]，存原因**文本** | 同 ✓（去空去重、限 20 条、每项 ≤50 字） | ✅ | 文本一致；迁移后不出现重复项 | 🔴 |
| F-5 | `room` | string，可空（`''`） | string?，可空（`null`） | ✅ 语义一致 | 空值在展示与聚合中的表现一致（见 D-3） | 🔴 |
| F-6 | `staffUsername` | string，可空 `''` | string?，可空 | ✅ | 一致 | 🔴 |
| F-7 | `staffName` | string（员工姓名） | **无此字段** | ❌ 缺 | 补上：提交时一并记录姓名，供明细与按工号统计显示 | 🔴 |
| F-8 | 备注 | 导出里有「备注」列（恒空） | 无 | 🟡 | 导出对齐时保留该列 | 🟠 |

### 9.2 用户/账号字段

| 编号 | 字段 | 旧系统 | 新系统 | 一致性验收标准 | 等级 |
|---|---|---|---|---|---|
| F-9 | 用户名 | `username` | `username` ✓ | 一致；迁移后旧账号可用原用户名登录 | 🔴 |
| F-10 | 姓名 | `name` | `displayName` | 映射正确，界面显示姓名 | 🔴 |
| F-11 | 英文名 | `nameEn` | 无 | ❓是否还需要（旧版仅后台用户列表用到） | ❓ |
| F-12 | 角色 | `manager` / `staff` | `admin` / `viewer` | 映射规则明确：manager→admin、staff→viewer | 🔴 |
| F-13 | 密码 | 明文存储 | PBKDF2-SHA256 | ⚪ 有意不同（安全）；迁移时需**重置密码**或让用户自设 | ⚪ |

### 9.3 接口返回结构

| 编号 | 接口 | 旧系统（本地函数/预留后端） | 新系统 | 一致性验收标准 | 等级 |
|---|---|---|---|---|---|
| A-1 | 提交评价 | `DB.addReview()`（本地写入） | `POST /api/review`（开放、含校验+限流） | 提交成功后客人看到与旧版一致的反馈；非法入参被拒且不污染统计 | 🔴 |
| A-2 | 明细 | `DB.getReviews()` / `queryReviews(opts)` | `GET /api/reviews?token=`（无参数） | 支持与旧系统同名的 5 个查询参数（日期/类型/原因/工号/房间）+ 分页 | 🔴 |
| A-3 | 统计 | `DB.statsBy(period,start,end)` 等 4 个函数 | `GET /api/stats`、`/api/stats/by-room`、`/api/stats/by-staff`（均无日期参数） | 补 `by-period`（天/周/月）与 `by-reason` 两个统计，且所有统计都接受日期范围 | 🔴 |
| A-4 | 设置读写 | `DB.getSettings()/saveSettings()` | `GET/PUT /api/settings`（admin）、`GET /api/settings/public`（开放） | 字段覆盖度对齐（见 B-34~B-38） | 🔴 |
| A-5 | 公开字段白名单 | 旧系统没有"公开/私密"之分（全在本地） | 公开接口只返回客人页需要的字段 | 保留（安全设计）：新增机密配置字段不得出现在 `/api/settings/public` | ⚪ |
| A-6 | 错误返回 | 无接口 | 401 未登录 / 403 需管理员 / 400 中文原因 / 429 限流（含中文提示与 CORS 头） | 前端能显示后端给的中文原因而不是英文技术文案 | 🔴 |

---

## 十、导出 / 导入 / 打印

| 编号 | 项目 | 旧系统（基线） | 新系统（现状） | 差异 | 一致性验收标准 | 等级 |
|---|---|---|---|---|---|---|
| E-1 | 交付格式 | **xlsx**（SheetJS），带样式化标题行与列宽 | **CSV**（带 BOM） | ❌ 格式不同 | 导出 xlsx；用 Excel 打开中文不乱码 | 🔴 |
| E-2 | 报表导出内容 | 3 个工作表：`统计报表`（标题`<酒店名> - 服务点评统计报表` + 周期/时间范围 + 表头`周期/好评数/差评数/合计/好评率`）、`差评原因`（表头`差评原因/次数`）、`按工号统计`（表头`工号/员工姓名/好评数/差评数/合计/好评率`）；**不含按房间统计** | 无（仅一个 CSV） | ❌ 缺 | 3 个 sheet 名称、标题行文案、表头与列顺序完全一致 | 🔴 |
| E-3 | 明细导出内容 | 1 个工作表 `评价明细`：标题`<酒店名> - 服务点评明细记录` + `导出时间：<本地 toLocaleString('zh-CN')>` + 表头 `时间/房间号/评价类型/差评原因/操作工号/员工姓名/备注`（7 列，备注恒空）；时间含**秒** `YYYY-MM-DD HH:mm:ss`；原因用 `、` 连接 | CSV 列：时间/类型/房间/原因/员工（5 列，时间无秒、原因用 `/` 连接） | ❌ 差异大 | 7 列齐全；标题与导出时间行存在；时间含秒；原因用「、」分隔 | 🔴 |
| E-4 | 文件名 | 报表：`<酒店名>_服务点评统计_YYYY-MM-DD.xlsx`；明细：`<酒店名>_评价明细_YYYY-MM-DD.xlsx` | `评价数据_YYYY-MM-DD.csv` | ❌ 不同 | 文件名格式与旧版一致（含酒店名与日期） | 🔴 |
| E-5 | 导出与筛选的关系 | 明细导出受**日期+类型+原因+工号**影响，**房间号筛选不参与**（旧版 bug）；报表导出仅受周期+日期影响 | 导出=当前全部已加载数据，与类型筛选也无关 | ❌ 不一致 | 见第十四节 Q12：导出是否跟随屏幕筛选 | ❓ |
| E-6 | 明细导出的日期默认值 | 默认近 30 天 → **不清空日期框直接导出会静默漏掉 30 天前的数据**（旧版 bug，也是数据迁移踩坑点） | 无此概念 | ❓ 旧版 bug | 建议改为"默认导出全部"或导出前提示 → 见 Q12 | ❓ |
| E-7 | 导出按钮权限 | 两个导出按钮都是 `manager-only`（前台角色看不到） | 「导出 CSV」按钮**无权限限制**，viewer 也能导 | ❌ 权限差异 | 与 Q9 拍板结果一致 | ❓ |
| E-8 | 导出成功提示 | toast「✅ 报表已导出」/「✅ 明细已导出」 | 无 | 🟡 | 导出后有明确提示 | 🟠 |
| E-9 | 导入 | **无 UI 导入功能**（只有从未启用的 `syncPull` 合并） | 无 UI 导入；有命令行迁移脚本 `migrate-from-old.mjs`（支持 json/xlsx/csv、默认预演、自动去重） | ⚪ 新增加能力 | 迁移脚本可用且不产生重复、不改动已有记录 | ⚪ |
| E-10 | 打印 | 全站**无打印样式、无打印按钮** | 二维码页有「🖨 打印全部」+ `@media print` 样式 | ⚪ 新增加能力 | 保留 | ⚪ |

---

## 十一、视觉与文案基线对照

### 11.1 设计令牌（色值）

| 编号 | 令牌 | 旧系统 | 新系统 | 一致性验收标准 | 等级 |
|---|---|---|---|---|---|
| V-1 | 主金色 | `#c9a84c`（浅 `#e8d5a3`、深 `#9a7a30`） | `#c9a84c` ✓（仅主色） | 主色一致；补浅/深金色用于边框与渐变 | 🔴 |
| V-2 | 好评色 | `#4a8c5e`（背景 `#e8f5ec`） | `#2d7a4a`（背景 `#eafaf0`） | **色值统一到 `#4a8c5e` / `#e8f5ec`** | 🔴 |
| V-3 | 差评色 | `#8c3d3d`（背景 `#f5e8e8`） | `#8c3333`（背景 `#fdecea`） | **色值统一到 `#8c3d3d` / `#f5e8e8`** | 🔴 |
| V-4 | 中性色阶 | 棕色系 10 级（`#2c1a0e` … `#faf5ef`）+ 灰阶 5 级 | 仅 `#333` / `#666` / `#888` / `#999` / `#eee` / `#f5f3ee` | 页面底色与卡片底色一致（旧：`#faf5ef` / `#fdf9f4`；新：`#f5f3ee` / `#fff`） | 🟠 |
| V-5 | 圆角 | `8 / 16 / 24 / 40px` 四级 | 6 / 8 / 10 / 12 / 16px 混用 | 卡片=16px、按钮=8px、大容器=24px | 🟠 |
| V-6 | 阴影 | 三级 + 金色光晕 `0 0 32px rgba(201,168,76,.3)` | 无阴影 | 卡片带阴影；LOGO 带金色光晕 | 🟡 |

### 11.2 字体与排版

| 编号 | 项目 | 旧系统 | 新系统 | 一致性验收标准 | 等级 |
|---|---|---|---|---|---|
| V-7 | 正文字体 | `PingFang SC, Microsoft YaHei, Segoe UI, sans-serif` | `-apple-system, PingFang SC, Microsoft YaHei, sans-serif` | 实际渲染一致（均为系统中文无衬线） | 🔴 |
| V-8 | 装饰字体 | Google Fonts：`Playfair Display`（数字/标题）、`Cormorant Garamond`（英文副标） | 无 | 英文副标与统计数字使用同款衬线字体 | 🟡 |
| V-9 | 客人页中文标题 | `STSong, SimSun, serif`（衬线） | 无此标题 | 与 K-3 一并还原 | 🟠 |
| V-10 | 布局宽度 | 客人页全屏居中；后台侧栏 240px + 内容区 | 全站 `max-width:720px` 居中 | 后台在宽屏下不出现"一条竖窄带"；客人页在平板上铺满 | 🟠 |
| V-11 | 响应式断点 | 900 / 768 / 640 / 360px 四级 | 720 / 560px，无分级 | 按第五节 K-7 与 B-4 的断点与表现对齐 | 🟠 |

### 11.3 固定文案（逐字对齐清单）

> 以下文案需**逐字**比对，含中英双语、emoji 与标点。

| 编号 | 位置 | 旧系统文案（基线） | 新系统现状 | 等级 |
|---|---|---|---|---|
| W-1 | 客人页主标题 | `服务点评` / `SERVICE REVIEW` | 缺 | 🔴 |
| W-2 | 客人页按钮 | `好评` / `Positive`、`差评` / `Negative` | `👍 好评` / `👎 差评`（无英文） | 🔴 |
| W-3 | 客人页语音按钮 | `语音提醒 Voice Prompt` | 缺 | 🔴 |
| W-4 | 差评屏标题 | `请告诉我们您的意见` / `PLEASE SHARE YOUR FEEDBACK` | 缺 | 🔴 |
| W-5 | 差评屏副标 | `您的反馈将帮助我们持续改进服务质量` / `Your feedback helps us improve our service quality` | 缺 | 🔴 |
| W-6 | 差评屏按钮 | `← 返回 Back`、`提交 Submit` | 缺（只有「提交评价」） | 🔴 |
| W-7 | 未选原因确认框 | `您未选择任何原因，确认提交吗？\nNo reason selected. Submit anyway?` | 缺 | 🔴 |
| W-8 | 感谢页（好评） | `感谢您的好评！` + `positiveMsg` + `positiveMsgEn` | 仅 `positiveMsg`（缺标题与英文） | 🔴 |
| W-9 | 感谢页（差评） | `感谢您的宝贵意见！` + `negativeMsg` + `negativeMsgEn` | 仅 `negativeMsg` | 🔴 |
| W-10 | 倒计时 | `X 秒后自动返回 / Returning in Xs...` | 缺 | 🔴 |
| W-11 | 房间号 | `🛏 当前房间 ROOM 802` | `房间号` + `802` | 🔴 |
| W-12 | 管理入口 | `⚙ 管理` | 顶部「后台」按钮 | 🟠 |
| W-13 | 后台看板卡片 | `今日好评`/`今日差评`/`今日合计`/`累计总评价`/`累计好评率` | `总评价`/`好评`/`差评`/`好评率` | 🔴 |
| W-14 | 看板区块标题 | `近7天趋势 / 7-Day Trend`、`好/差评占比 / Rating Distribution`、`差评原因排行 / Top Negative Reasons`、`🛏 差评房间预警 / 近30天 Top Rooms`、`客房部重点关注` | 缺 | 🟠 |
| W-15 | 报表区块标题 | `统计图表`、`汇总数据`、`按工号统计 / By Staff Account`、`按房间统计 / By Room（有评价记录的房间）` | 缺 / `📊 按房间统计（差评优先）`、`📊 按员工工号统计` | 🔴 |
| W-16 | 报表汇总行 | `合计：好评 X / 差评 Y / 好评率 Z%` | 缺 | 🔴 |
| W-17 | 记录页标题与计数 | `评价明细记录`、`共 N 条记录` | 缺（表格无标题与计数） | 🟠 |
| W-18 | 空态文案 | `暂无数据`、`暂无差评数据`、`近30天无差评记录 🎉`、`暂无数据（评价记录中暂无工号信息）` | 仅 `暂无数据` | 🟠 |
| W-19 | 设置区标题 | `🏨 酒店基础信息`、`🔊 语音提醒设置`、`💬 感谢语设置`、`📡 捷信达 PMS 接口`、`☁️ 数据聚合后端（跨设备汇总）`、`💾 保存设置 Save` | `⚙️ 站点设置`、`保存设置` | 🟠 |
| W-20 | 账号管理标题 | `账号管理 / User Management`、`＋ 新增账号`、`📋 差评原因管理`、`＋ 添加差评原因`、`👥 系统账号` | `👤 账号管理`、`新增账号` | 🟠 |
| W-21 | 角色文案 | `店总 Manager（完整权限）`、`前台 Front Desk（查看+无导出）`、`店总 Manager`、`前台 Front Desk` | `管理员`、`查看者` | 🔴 |
| W-22 | 登录页文案 | `账号 Username`、`密码 Password`、`登 录　LOGIN`、`账号或密码错误 / Incorrect username or password` | `用户名`、`密码`、`登 录`、`登录失败，请检查网络或账号密码` | 🔴 |
| W-23 | 二维码页文案 | `房间二维码下载`、`扫码进入服务点评 · 一房一码`、`搜索房间号，如 601、1802A…`、`下载全部（ZIP）`、`共 N 个房间二维码`、`匹配 N / 共 M 个房间`、`没有匹配的房间号`、`下载 PNG`、`复制链接`、`城市酒店服务点评系统 · 返回点评页` | 仅 `⬇ 下载` 与 `🖨 打印全部` | 🟠 |
| W-24 | 不可删除提示 | 源码注释明示"任何人（包括店总/管理员）都没有删除评价的权限" | `🔒 评价数据只读，任何人都不可删除` ✓ | 🔴 |

---

## 十二、角色与权限对照

| 编号 | 能力 | 旧系统 manager / staff | 新系统 admin / viewer | 一致性验收标准 | 等级 |
|---|---|---|---|---|---|
| R-1 | 查看看板/报表/明细 | ✅ / ✅ | ✅ / ✅ | 一致 | 🔴 |
| R-2 | 导出 Excel | ✅ / ❌ | ✅ / **✅（当前允许）** | 默认应与旧版一致：viewer 不可导出 → 见 Q9 | ❓ |
| R-3 | 改系统设置（酒店名/文案/原因） | ✅ / ❌ | ✅ / ❌ | 一致 | 🔴 |
| R-4 | 上传 LOGO | ✅ / ❌ | ✅ / ❌ | 一致 | 🔴 |
| R-5 | 账号管理 | ✅ / ❌ | ✅ / ❌ | 一致 | 🔴 |
| R-6 | 删除评价 | ❌ / ❌（代码层禁用） | ❌ / ❌（后端无删除接口、前端无按钮） | 一致，且必须保持 | 🔴 |
| R-7 | 客人提交评价 | 免登录 | 免登录 | 一致，且必须保持 | 🔴 |
| R-8 | 停止使用某账号 | 只能删除 | 可停用（立即踢会话） | ⚪ 新增加能力，保留 | ⚪ |
| R-9 | 主令牌后门 | 无（本地系统无需） | 有（防忘记密码锁死） | ⚪ 保留 | ⚪ |
| R-10 | 登录态存储 | localStorage 明文含密码 | localStorage 只存令牌（PBKDF2 存库） | ⚪ 有意不同（安全） | ⚪ |

---

## 十三、有意不同（不需要对齐，但需客户知悉）

| 编号 | 项目 | 说明 |
|---|---|---|
| X-1 | **数据集中存储** | 旧系统评价只存在**提交设备自己的浏览器**里（换台电脑看不到）；新系统全部进 PostgreSQL，任何设备都能看到同一份数据。这是本次重构的**根本目的** |
| X-2 | **跨设备汇总内置** | 旧系统有一个"跨设备汇总后端"但从未启用（`server/data/` 里只有 `.gitkeep`）；新系统的汇总能力是天然的，不需要那个开关 |
| X-3 | **公开接口加固** | 新系统对 `POST /api/review` 做了入参校验（类型白名单、长度、去重）与按真实 IP 的令牌桶限流（突发 30 / 每分钟回补 12）；旧系统没有（本地存储，不存在被刷风险） |
| X-4 | **密码加密与会话令牌** | 旧系统密码明文存在 localStorage 且会话对象含密码；新系统 PBKDF2-SHA256 存库、会话只存令牌、30 天有效、停用/改密立即踢会话 |
| X-5 | **默认账号提示已移除** | 旧登录页直接写「默认账号：admin / staff ｜ 密码：admin123 / staff123」；新系统不显示（安全考虑），初始管理员密码 = `AdminToken` |
| X-6 | **前端不依赖外部 CDN** | 旧系统图表/Excel/JSZip 都从 CDN 加载（断网即失效）；新系统不依赖 CDN |
| X-7 | **PMS / 聚合后端两个设置区** | 旧系统这两个区一个是禁用占位、一个从未启用；新系统未实现（是否要保留占位展示见 Q7） |

---

## 十四、需要你拍板的问题清单（优先看这一节）

| 编号 | 问题 | 背景 | 选项 A | 选项 B | 我的建议 |
|---|---|---|---|---|---|
| **Q1** | **好评要不要选原因？** | 旧系统「好评」是**点一下就提交**、没有原因；而你此前明确选了"好评选项可后台配置"。这两者直接冲突 | **严格一致**：好评一键提交，去掉好评原因选项（后台也不再配好评选项） | **保留增强**：好评也可选原因（客人可跳过直接提交） | 选 B 但**必须让"不选原因"也能一键提交**（例如点「👍 好评」即提交，另有"补充原因"入口），兼顾一致与增强 |
| **Q2** | 防重复提交要不要保留？ | 旧系统连点好评会写入多条 | 照旧（不防） | 保留新系统的提交锁（提交中禁用按钮） | 保留新系统（这是明确的缺陷修复，不是功能差异） |
| **Q3** | 前台平板（无 `?room=`）房间号怎么办？ | 旧系统留空；新系统提供手填框 | 照旧留空 | 保留手填框（可不填） | 保留手填（能补录，且不影响一致） |
| **Q4** | "今日/近 7 天"按本地日还是 UTC？ | 旧系统用 UTC 日，东八区 0–8 点会错一天 | 照旧 UTC（严格一致） | 按本地日（正确） | 按本地日；这属于修 bug，不是改功能 |
| **Q5** | 报表页三个"看着有但不生效"的筛选（类型/原因/工号）怎么办？ | 旧系统读了值但查询没用上 | 照旧（摆着不生效） | **真的让它生效** | 让它生效（否则是误导用户） |
| **Q6** | 差评原因管理用哪种交互？ | 旧系统=每条可增删改+启用开关+英文名；新系统=一个多行文本框整段覆盖 | 复刻旧版（列表式增删改 + 启用开关） | 保留文本框（简单，但会**覆盖式删除**，误操作会丢配置） | 复刻旧版列表式；至少要有"启用/停用"（否则改配置会误删历史在用选项） |
| **Q7** | 「📡 捷信达 PMS 接口」这个禁用占位区要不要保留？ | 旧系统有（disabled 展示，标注"预留接口 · 待对接"） | 保留占位（客户看到"以后能对接"） | 去掉（未实现就别展示） | 保留占位，但要明确标注"预留、尚未对接" |
| **Q8** | 登录页的"默认账号密码"提示要不要恢复？ | 旧系统明示；新系统已移除 | 恢复（前台人员省事） | 不恢复（安全） | 不恢复；改为**纸质交付卡**上写账号，不写在网页上 |
| **Q9** | 「前台/查看者」能不能导出？ | 旧系统 staff **不能**导出；新系统 viewer 能导出 | 与旧版一致：viewer 不可导出 | 允许 viewer 导出 | 与旧版一致（不可导出），避免权限越界 |
| **Q10** | 好评率显示几位小数？ | 旧系统屏幕整数（33%）、导出 1 位（33.3%）；新系统 1 位 | 统一整数 `33%` | 统一 1 位小数 `33.3%` | 统一 **1 位小数**（导出早已是 1 位，屏幕跟导出走） |
| **Q11** | "周"从周日还是周一开始？ | 旧系统周起始用周日、周号用 ISO（周一），两套口径混用 | 周日起算 | ISO（周一起算） | ISO 周一（与旧系统"周号"显示一致，避免 `2026-W37` 与实际周不符） |
| **Q12** | 导出是否跟随屏幕上的筛选？ | 旧系统：明细导出忽略"房间号"筛选、默认只导近 30 天（会静默漏历史）；报表导出只受日期影响 | 照旧（含这些不一致） | **明确规则**：导出 = 当前屏幕筛选结果的完整集合，且默认为"全部时间" | 选 B，并在导出按钮旁标注"将导出 N 条" |
| **Q13** | 语音播报（30 秒首次、每 60 秒重复）要不要保留？ | 旧系统有；新系统没有 | 保留（平板自动播报招徕客人） | 去掉（怕吵/怕打扰） | 保留但**默认关闭**，后台可开（若保留则必须支持配置） |
| **Q14** | 客人页要不要做真正的语言切换？ | 旧系统设置里有 `lang` 字段但**从未被使用**，实际是"中英同屏" | 中英同屏（照旧，不切换） | 做 zh/en/both 三态切换（让 `lang` 字段真正生效） | 先做"中英同屏"（照旧）；`lang` 切换列为 P2 |
| **Q15** | 台卡设计稿还要不要？ | 旧仓有一份 `台卡设计稿.html`（给广告店印亚克力立牌用） | 迁到新仓 | 不需要（已有印刷成品） | 按实际是否要重印决定 |

---

## 十五、施工顺序建议

> 改动量 S = 小（单文件、几小时级）、M = 中（跨前后端）、L = 大（涉及表结构或新接口）。
> 等级：🔴P0 必须先做（不做不算交付）、🟠P1 次之、🟡P2 收尾。

### 第 1 批 · 数据查询与字段（改完后端才算"能用"）🔴

| 项 | 改动量 | 涉及 | 依赖 |
|---|---|---|---|
| B-19 日期范围筛选（明细） | L | 后端 `GET /api/reviews` 加 `start/end`，前端加日期控件 | — |
| B-21/22/23 原因/工号/房间筛选 | M | 同上（加 3 个查询参数） | B-19 |
| B-24 分页（20/页） | M | 后端 `page/pageSize` + 前端页码 | B-19 |
| B-31 性能（避免全量加载） | M | 上面几项做完即解决 | — |
| F-7 补 `staffName` 字段 | M | 表结构 + 提交/返回/展示 | 需数据库迁移 |
| B-12/13/15 报表页（周期+日期+汇总表） | L | 新增 `GET /api/stats/by-period` | B-19 |
| S-10 所有统计支持日期范围 | M | 3 个统计接口加参数 | B-19 |
| B-16/17 按工号补姓名、按房间补好评率+颜色 | S | 前端 + 后端返回 | F-7 |
| D-5 好评率精度统一 | S | 前后端 | — |
| B-26 时间格式统一 `YYYY-MM-DD HH:mm` | S | 前端格式化 | — |

### 第 2 批 · 客人页体验复刻 🔴

| 项 | 改动量 | 说明 |
|---|---|---|
| K-1/K-9/K-10/K-11/K-15 三屏流程 + 好评一键提交 | L | 客人页重构（等 Q1 拍板） |
| K-3/K-4 主标题 + 装饰 | S | 纯样式 |
| K-5/K-18/W-1~W-12 中英双语 + 语音 | M | 需后台新增英文字段与语音配置 |
| K-13 未选原因确认框 | S | — |
| K-16/K-17 感谢屏 + 可配置倒计时 | M | 图形（星形/机器人）+ 倒计时 + 配置项 |
| K-19 时钟 | S | — |
| K-6/K-20/W-11 房间号与管理入口 | S | — |
| K-7 移动端断点适配 | M | CSS |

### 第 3 批 · 设置与账号 🟠

| 项 | 改动量 | 说明 |
|---|---|---|
| B-34~B-36 语音文案(中英)/感谢语英文/倒计时秒数 | M | `AppSettings` 加字段 + 后台表单 + 客人页消费 |
| B-37/B-38 差评原因列表式管理 + 英文名 + 启用开关 + 默认标签对齐 | L | 新的原因表 + 接口 + UI |
| B-32/B-33 设置分区结构（含 LOGO 归位到"酒店基础信息"） | M | 前端重构 |
| W-21~W-23 文案逐字对齐 | M | 前端文案 |
| B-1/B-2 侧边栏 + 5 页 + 顶栏（含时钟、前台展示） | L | 前端信息架构 |
| B-43/B-47/B-48 登录页与账号管理文案/交互 | M | 前端 |
| R-2/Q9 导出权限 | S | 前端 `v-if` |

### 第 4 批 · 导出与看板 🟠

| 项 | 改动量 | 说明 |
|---|---|---|
| E-1~E-4 导出改 xlsx（多 sheet/标题行/列宽/文件名） | L | 前端引入 SheetJS（本地依赖，不用 CDN） |
| E-5/E-6/Q12 导出范围与默认值规则 | M | 依赖 B-19 |
| B-6 看板 5 张卡（含今日） | S | 依赖后端今日统计 |
| B-7/B-8/B-9/B-10 4 个图表/排行榜/预警表 | L | 引入图表库（本地打包） |
| B-27/B-29/W-18 计数与空态文案 | S | 前端 |

### 第 5 批 · 视觉与运营工具 🟡

| 项 | 改动量 | 说明 |
|---|---|---|
| V-1~V-6 色值/圆角/阴影/字体对齐（抽成 CSS 变量） | M | 纯样式，但影响面广 |
| V-10/V-11 布局宽度与断点 | M | — |
| Q1~Q11 二维码页补齐（搜索/ZIP/复制/预览/计数/页脚） | M | 前端（可复用旧版 JSZip 逻辑） |
| B-52/W 系列剩余文案 | S | — |
| T-1 台卡（若需要） | S | — |

---

## 附录 A · 验收用抽样核对法（每次改完都跑一遍）

**准备**：一份包含边界情况的数据集，建议至少 40 条，必须覆盖：

| 场景 | 举例 | 为什么要 |
|---|---|---|
| 含字母房号 | `1801A` / `1802B` | 大小写与筛选匹配 |
| 房间号为空 | 前台模式提交 | 「未记录」聚合 |
| 工号为空 | 客人手机扫码提交 | 「未记录」聚合 |
| 原因多选 | 选 3 个原因 | 原因筛选与导出连接符 |
| 原因为空的好评 | 好评 | 空值显示 `-` |
| 跨天/跨月/跨年 | 8/31、9/1、12/31、1/1 | 周期聚合与周边界 |
| 同一天多条 | 同日 10 条 | 今日统计与分页 |
| 30 天前的老数据 | 3 个月前 | 日期默认值与"漏导"风险 |

**核对步骤**：

1. 同一份数据分别在新旧系统可查（旧系统用导出文件导入对照，或直接在旧系统录入同样内容）。
2. **明细逐行比对**：条数 → 每行 5（或 7）个字段 → 排序。任何一行不一致即不通过。
3. **统计逐项比对**：总数、好评、差评、好评率、按房间每行 5 个数字、按工号每行 6 个数字。
4. **日期边界专项**：选"9月12日～9月12日"，必须能查到当天 00:00:01 与 23:59:59 的记录。
5. **导出比对**：新旧导出文件用 Excel 打开，比对 sheet 名、标题行、表头、行数、每格内容、文件名。
6. **权限比对**：用前台/查看者账号登录，确认看不到的菜单、按钮与旧系统一致。
7. **客人页操作序列比对**：好评（不选原因）→ 差评（不选原因，弹确认框后取消）→ 差评（选 2 个原因再提交）→ 观察感谢页与倒计时。
8. **不回归检查**：评价仍不可删除；客人免登录仍能提交；一房一码链接仍能锁定房间号；手机流量（非内网）仍能提交。

**通过判定**：P0 项全部通过 + 附录 A 的 8 步全部通过 + 无新增回归项 = 本轮验收通过。

---

## 附录 B · 旧系统已知 bug 清单（对齐时的取舍）

> 这些是旧系统本身的问题。**"完全一致"不应该等于"把 bug 也复制过来"**，请你逐条确认取舍。

| 编号 | 问题 | 证据 | 建议 |
|---|---|---|---|
| BUG-1 | 报表页「类型/差评原因/操作工号」3 个筛选控件读了值却没参与查询（不生效） | `admin.html:944-947` 读值，`949/991/1010` 的查询调用都没传这三个参数 | **不复制**：新系统让它真的生效（Q5） |
| BUG-2 | 明细导出默认近 30 天，不清空日期直接导出会静默漏掉更早的数据 | `admin.html:1030-1036`（默认填日期）+ `1377-1378`（导出直接读输入框） | **不复制**：默认"全部时间"或导出前提示条数（Q12） |
| BUG-3 | 明细导出忽略「房间号」筛选 | `admin.html:1379-1382` 未读 `#rec-room` | **不复制**：导出跟随屏幕筛选（Q12） |
| BUG-4 | 「今日」与"近 7 天"用 `toISOString()` 取日期（UTC），东八区 0–8 点会错一天 | `admin.html:823, 844-850` | **不复制**：按本地日（Q4） |
| BUG-5 | "周"的起始日（周日）与周号（ISO 周一）两套口径混用 | `js/db.js:144-147` + `377-383` | 统一 ISO（Q11） |
| BUG-6 | 移动端后台 `☰` 按钮写死 `display:none` 且无 JS 打开 → ≤640px 侧边栏进不去 | `admin.html:300, 817-819`；`style.css:376-380` | **不复制**：移动端必须能进所有页面 |
| BUG-7 | 客人页无防重复提交，连点写入多条 | `index.html:779-782`；`js/db.js:84` | **不复制**：保留新系统提交锁（Q2） |
| BUG-8 | 评价 `id = Date.now()`，同毫秒可能撞号 | `js/db.js:82` | **不复制**：新系统用 Guid |
| BUG-9 | 明细表空态文案 `colspan=6` 与 5 列表头不一致 | `admin.html:502, 1073` | **不复制** |
| BUG-10 | 设置项 `lang` 定义但全站未使用（形同虚设） | `js/db.js:56`，无消费点 | 要么去掉，要么让它生效（Q14） |
| BUG-11 | 前后台默认 LOGO 用了不同文件（`logo-icon.png` vs `logo.jpg`） | `index.html:601`；`admin.html:221` | 统一为同一个 |
| BUG-12 | 密码明文存 localStorage，会话对象含密码 | `admin.html:729`；`js/db.js:24-27` | **不复制**（新系统已加密） |
| BUG-13 | README 写"支持筛选和删除"，实际删除已被移除 | `README.md:50` vs `admin.html:1089-1090` | 修文档 |
| BUG-14 | 聚合后端代码完整但从未启用（无任何数据落盘） | `server/data/` 只有 `.gitkeep`；`syncEnabled:false` | 新系统不需要（已内置集中存储） |

---

## 附录 C · 数据迁移期的两个额外一致性风险

| 编号 | 风险 | 说明 | 处理建议 |
|---|---|---|---|
| M-1 | **原因标签集不一致** | 旧数据里的原因是旧标签（`服务差`、`卫生差`、`等待太久`…），新系统默认标签是另一套（`服务态度差`、`房间不干净`…）。迁移后：明细里能看到旧文本，但**筛选下拉里没有这些选项**，按原因筛选会漏 | 二选一：① 把新系统默认差评原因**改成旧系统的 8 条**（推荐，历史数据天然可筛）；② 迁移时做标签映射（需你确认映射表） |
| M-2 | **用户密码无法迁移** | 旧系统密码明文（可迁移）但新系统是 PBKDF2 哈希。技术上可以"迁移时按明文重新哈希"，但旧密码强度太低（`admin123`） | 账号**重建**：迁移后为每个工号新建账号、设置新密码；旧账号 `admin`/`staff` 映射为 `管理员`/`查看者` |
| M-3 | **同域 localStorage 已隔离** | 新旧系统同在 `wx277282356.github.io` 下、共用浏览器存储。已修：新系统改用 `hrv2_session` 键，不再与旧系统的 `hr_session` 互相覆盖 | 已解决（commit `1c12240`）；迁移期先导数据还是先登录新系统都可以 |

---

*本清单由新旧两套系统的源码逐项对照产出；标注"旧系统"的行均可在 `hotel-review/` 中找到对应实现，标注"新系统"的行对应 `hotel-review-v2/`。*
