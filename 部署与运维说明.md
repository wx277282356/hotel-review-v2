# 城市酒店 · 客房点评系统 — 部署与运维说明

> 技术栈：前端 Vue3 + 后端 .NET 10 + 数据库 PostgreSQL（前后端分离，GitHub 统一管理）
> 线上前端：https://wx277282356.github.io/hotel-review-v2/
> 仓库：https://github.com/wx277282356/hotel-review-v2

---

## 一、架构速览（为什么这样部署）

```
客人手机扫码
   │  二维码指向：https://wx277282356.github.io/hotel-review-v2/?room=房间号
   ▼
前端（GitHub Pages 静态托管，仅放页面，不能跑后端）
   │  页面里的 config.js 指定 apiBase = 后端公网地址
   ▼
后端（跑在酒店本地常开机器，.NET 10，端口 5188）
   │  经 Cloudflare Tunnel 暴露到公网 https 地址
   ▼
PostgreSQL（同一台本地机器，库名 hotel_review，评价只增不删）
```

**关键约束（已落实）**
- 评价**任何人都不可删除**：后端无删除接口，前端无删除按钮。
- 房间号从二维码 URL `?room=房间号` 自动锁定，客人在页面上看得到、改不了。
- 所有评价集中存在本地 PostgreSQL，任意设备（手机/平板）都能在后台查到汇总。

---

## 二、本机自测（开发模式）

```bash
# 后端（终端1）
cd backend && dotnet run          # http://localhost:5188

# 前端（终端2）
cd frontend && npm run dev        # http://localhost:5173
```

开发模式前端走 vite 代理 `/api -> localhost:5188`，无需公网即可联调。

### ⚠️ 密钥配置（务必先做，且不要提交到 Git）

本仓库是**公开**的，所以真实密码**绝不能**写在会被提交的文件里。

| 文件 | 是否提交 | 放什么 |
|---|---|---|
| `backend/appsettings.json` | ✅ 会提交 | **只有占位符**，不含任何真实密码 |
| `backend/appsettings.Local.json` | ❌ 已 gitignore | **真实**数据库密码 + 管理员令牌 |

首次使用若没有 Local 文件，把 `appsettings.json` 复制一份改名为 `appsettings.Local.json`，
再把里面两处 `CHANGE_ME` 换成真实值即可。后端启动时会自动加载它并覆盖模板值。

```jsonc
// backend/appsettings.Local.json 示例
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=hotel_review;Username=postgres;Password=你的库密码"
  },
  "AdminToken": "一段只有你知道的随机字符串",
  "AllowedHosts": "*"
}
```

> - 没配好连接串时，后端会直接打印醒目错误并退出，不会带着坏配置硬跑。
> - `AdminToken` 是**管理员后门**（详见第九节），等于你的万能钥匙，务必改掉默认值。

> ⚠️ **关于 Git 历史**：如果真实密码曾经写进 `appsettings.json` 并推送过（本项目早期版本就是如此），
> 那即使现在从文件里删掉，**旧密码依然留在 Git 历史里、公开可查**。
> 删文件不能止损，**唯一有效的补救是把那个密码换掉**（换数据库密码 / 换 `AdminToken` / 换后台登录密码）。
> 本项目已完成密钥轮换。

> 🔑 **后台需要登录**（详见第九节「账号与权限」）。
> 首次启动会自动创建管理员账号 **`admin`**，初始密码 = `appsettings.Local.json` 里的 `AdminToken`
> （若未配置则随机生成并在控制台显示一次）。
> 登录后请立刻在后台「修改密码」里改掉。

---

## 三、正式上线三件套

已为你做好的（在本项目里，可重复执行）：
1. ✅ 前端已部署到 GitHub Pages（改完代码 `npm run deploy` 即更新）。
2. ✅ 221 间房二维码已生成在 `frontend/public/qrcodes/`，并在线可下载：
   https://wx277282356.github.io/hotel-review-v2/qrcodes/
3. ✅ 后端代码、数据库脚本已就绪。

**你需要在自己电脑（酒店常开机器）上做的：双击 `一键启动.bat`（就这一步）。**

详见下一节；第四节的手工流程只在自动流程失败时才需要。

---

## 三·五、一键启动 ★日常只用这一个

**双击项目根目录的 `一键启动.bat`**，它会自动依次完成：

| 步骤 | 做什么 |
|---|---|
| 1 | 检查后端是否已在跑；没跑就启动它（首次编译约 20–60 秒，会等它就绪） |
| 2 | 启动 Cloudflare 隧道，从输出里自动抓取公网 https 地址 |
| 3 | 把该地址自动写进 `frontend/public/config.js` 的 `apiBase` |
| 4 | 自动执行 `npm run deploy` 发布前端到 GitHub Pages |
| 5 | 打印一份汇总（前端地址 / 后台登录页 / 后端公网地址） |

跑完看到 `[启动器] 全部就绪 🎉` 就成了。**窗口保持开着**，关掉窗口或按 Ctrl+C 会一并停掉后端与隧道。

**背后跑的命令**（懂技术的人可单独用）：

```bash
node scripts/start-all.mjs                # 一键启动
node scripts/start-all.mjs --no-tunnel    # 只启后端（本地调试）
node scripts/start-all.mjs --no-deploy    # 起隧道但不自动发布
```

**要点**

- 脚本已内置「清代理变量 + 补 .NET PATH + 自动找 Node」，所以不受本机环境变量影响。
- 隧道地址是**临时地址、每次重启都会变**；脚本会自动换成新地址并重新发布，你不用管。
- 若某一步失败，脚本会明确告诉你是哪一步、以及手动补救命令（不会假装成功）。
- `start-backend.bat` / `start-tunnel.bat` 仍保留，作为分步手动执行的备选。

> ✅ **本链路已实测跑通**（2026-09-12）：`cloudflared` 已下载好放在项目根目录，
> 用 `cloudflared tunnel --url http://localhost:5188` 起隧道后拿到公网地址，
> 经该地址 `POST /api/review` 提交的评价成功落进本地 PostgreSQL，统计接口同步正确。
> 跨域（CORS）实测通过：预检返回 204、`Access-Control-Allow-Origin: *`。

> ⚠️ **两个坑（实测踩到，务必注意）**
> 1. **代理会拦隧道**：如果本机设了 `HTTP_PROXY/HTTPS_PROXY`（如公司代理/加速器），
>    cloudflared 可能连不上。启动前先清掉代理变量：
>    ```bash
>    unset HTTP_PROXY HTTPS_PROXY http_proxy https_proxy
>    export NO_PROXY="*"
>    ./cloudflared.exe tunnel --url http://localhost:5188
>    ```
> 2. **下载 cloudflared 别写死代理端口**：若代理端口变了，用旧端口下载会一直失败；
>    加 `--noproxy '*'` 直连最快：
>    ```bash
>    curl -sL --noproxy '*' -o cloudflared.exe \
>      https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe
>    ```

> 隧道启动后终端会打印连通性自检表；本环境显示 QUIC 可用、TCP 7844 被墙，
> 属**降级但可用**（会走 quic），不影响正常使用。

---

## 四、设置后端公网地址（apiBase）★关键一步

前端页面并不知道后端在哪，靠 `frontend/public/config.js` 这一个文件指定：

```js
window.__APP_CONFIG__ = {
  apiBase: ''   // 改成：'https://xxxx.trycloudflare.com/api'
};
```

改完后：
```bash
cd frontend
npm run deploy     # 自动构建并推送到 GitHub Pages，约 1 分钟生效
```
之后客人扫码提交，数据就会经隧道落到本地 PostgreSQL。

> 小技巧：config.js 是运行时读取的，但它在 GitHub Pages 分支上，
> 所以改完必须 `npm run deploy` 重新发布（无需改源码、无需重新构建前端打包）。

---

## 五、生成 / 重生成二维码

房间清单在 `frontend/scripts/rooms.txt`（每行一个房间号）。
换域名时改 `frontend/scripts/gen-qrcodes.mjs` 顶部的 `SITE_BASE`，然后：

```bash
cd frontend
node scripts/gen-qrcodes.mjs     # 生成到 public/qrcodes/
npm run deploy                   # 发布到线上下载页
```

下载/打印页：https://wx277282356.github.io/hotel-review-v2/qrcodes/
（可点「🖨 打印全部」批量打印后裁剪，贴于各客房）

---

## 六、微信限制 & 正式域名

- ❗ **github.io 域名会被微信拦截**（客人微信里扫码会弹出"申请恢复网页"）。
  电脑/手机浏览器打开正常，但客人用微信扫码的真实流程要等换成客户自有域名后才跑通。
- 跑通测试后可让客户申请一个自有域名（如 `review.客户酒店.com`），
  把该域名 CNAME/解析到 Cloudflare Tunnel 的命名隧道地址，
  再改一次 `SITE_BASE` 与 `apiBase`、重跑二维码与 `npm run deploy` 即可。

---

## 七、旧系统数据迁移（localStorage → PostgreSQL）

旧纯静态系统的评价存在**每台设备自己的浏览器 localStorage**（按设备隔离），迁移需每台提交过评价的设备各导一次：

**1) 导出（在旧系统那台设备/浏览器上操作）**
- 打开旧点评页 https://wx277282356.github.io/hotel-review/
- 按 F12 → Console（控制台），把 `frontend/scripts/export-old-reviews.snippet.js` 里的整段代码粘贴进去回车
- 浏览器自动下载 `hotel-review-old-reviews.json`（包含该设备的全部历史评价）

**2) 导入（在本机，后端已在跑时）**
```bash
cd frontend
node scripts/migrate-from-old.mjs <导出的json文件路径>
```
- 脚本直接写 PostgreSQL，会**原样保留每条评价的真实提交时间**（不走 /api/review，避免时间被刷成导入当下）
- 旧 type 取值 positive/negative 与新库一致；good/bad 会自动归一化
- 导入后后台页 `?admin=1` 登录查看，或 `GET /api/reviews?token=<你的登录令牌>` 校验

**3) 多设备**
每台平板/电脑各导出一个 json，分别跑一次导入即可（不同设备的评价是各自真实的提交，无需去重）。

> 参考样例：`frontend/scripts/sample-old-reviews.json`（旧系统导出结构示例，仅作格式说明，非真实数据）

---

## 八、后台查看汇总

浏览器打开：https://wx277282356.github.io/hotel-review-v2/?admin=1
用账号登录（管理员 `admin`）查看统计与明细、导出 CSV。
忘了密码就在登录页点「用主令牌登录（管理员后门）」，令牌见 `backend/appsettings.Local.json` 的 `AdminToken`。

后台包含三块：
1. **总览卡片 + 评价明细**（按好评/差评筛选，可导出 CSV）
2. **📊 按房间统计**（差评优先排序，方便快速定位问题房）
3. **📊 按员工工号统计**（按评价量排序）

### 品牌 LOGO 上传（酒店可自行更换）

后台页顶部有「🏨 品牌 LOGO」区块，酒店管理人员可自助操作：

| 操作 | 说明 |
|---|---|
| 选择图片并上传 | 支持 png / jpg / webp / gif，**建议正方形、≤2MB** |
| 恢复默认 | 清除自定义 LOGO，回到内置默认图 |

要点：
- 上传的 LOGO 存在**后端机器的 `backend/branding/logo.*`**，不在 Git 里；换机/重装前请备份该目录。
- 上传后**客人扫码页与后台同步生效**（客人端刷新一次即可）。
- 上传/恢复需要先填写管理员令牌（即 `admin_token`）。

> 说明：客房模式下提交时，工号自动取当前房间号；若后续接入前台工号（如 `?room=1001&staff=0123`），按工号统计即可直接反映到人。

### 后端接口一览（除提交流程外均需带 `?token=`）

| 方法 | 路径 | 作用 |
|---|---|---|
| POST | `/api/review` | 提交一条评价（**开放，无需令牌**）。会校验：`type` 必须是 `positive`/`negative`（大小写自动归一）；`room`、`staffUsername` 限 50 字；`reasons` 自动 trim、去空、去重、最多 20 条 |
| GET | `/api/reviews?token=` | 全部评价明细 |
| GET | `/api/stats?token=` | 总览统计（总数/好评/差评） |
| GET | `/api/stats/by-room?token=` | 按房间分组统计 |
| GET | `/api/stats/by-staff?token=` | 按工号分组统计 |
| GET | `/api/settings/logo` | 读取当前品牌 LOGO（**开放**，无自定义时返回 404） |
| POST | `/api/settings/logo?token=` | 上传/替换品牌 LOGO（multipart，字段名 `file`）**仅管理员** |
| DELETE | `/api/settings/logo?token=` | 恢复默认 LOGO **仅管理员** |
| POST | `/api/auth/login` | 登录（body：`username`/`password`）→ 返回会话令牌，**开放** |
| GET | `/api/auth/me?token=` | 当前登录身份 |
| POST | `/api/auth/logout?token=` | 退出登录（销毁会话） |
| POST | `/api/auth/password?token=` | 自助改密（body：`oldPassword`/`newPassword`） |
| GET | `/api/staff?token=` | 账号列表 **仅管理员** |
| POST | `/api/staff?token=` | 新增账号 **仅管理员** |
| PATCH | `/api/staff/{id}?token=` | 改姓名/角色/启停/重置密码 **仅管理员** |
| DELETE | `/api/staff/{id}?token=` | 删除账号 **仅管理员** |

> 注意：后端**没有删除接口**，这是刻意的设计——评价一旦提交，任何人都无法删除。

> 说明：提交接口是公开的（客人免登录），所以做了入参校验，挡住"没有意义的请求"。
> 注意 `room` 允许为空是**有意设计**（前台模式可以没有房间号），不要误当成缺失。

---

## 九、账号与权限（谁可以看什么）

### 两种角色

| 能力 | 管理员 admin | 查看者 viewer |
|---|:---:|:---:|
| 看统计卡片 / 评价明细 | ✅ | ✅ |
| 按房间、按工号统计 | ✅ | ✅ |
| 导出 CSV | ✅ | ✅ |
| 上传 / 恢复默认 LOGO | ✅ | ❌ |
| 新增 / 停用 / 删除账号 | ✅ | ❌ |
| 重置他人密码 | ✅ | ❌ |

> 前台给「查看者」账号即可：能看数据、能导出，但改不了任何配置。

### 初始账号

首次启动后端会自动创建：
- 用户名 **`admin`**，密码 = `backend/appsettings.Local.json` 里的 **`AdminToken`**
  （首次启动时后端控制台也会打印出来；若未配置则随机生成并在控制台显示一次）
- 登录后请立刻点右上「修改密码」改掉，改完旧密码立即失效

### 忘密码怎么办（不会锁死）

`appsettings.json` 的 **`AdminToken` 始终是管理员后门**——它永远能登录、且拥有管理员权限。
所以哪怕所有账号密码都忘了，也能：
1. 用主令牌登录（登录页点「用主令牌登录（管理员后门）」）
2. 进「账号管理」重置任意账号密码

> 建议：正式交付后把 `AdminToken` 改成一段只有你自己知道的随机字符串，它就等于你的万能钥匙。

### 日常操作

- **加一个前台账号**：账号管理 → 填登录名/姓名/初始密码 → 角色选「查看者」→ 新增账号
- **员工离职**：账号管理里点「停用」——**该账号的所有登录会立即失效**，历史评价不受影响
- **换人接手**：点「重置密码」给他一个新密码即可
- **误删账号**：只删登录权限，**评价数据一条都不会少**

### 安全说明（已实测）

| 场景 | 结果 |
|---|---|
| 密码错误 / 用户不存在 | 统一提示"用户名或密码错误"，不暴露账号是否存在 |
| 密码存储 | PBKDF2-SHA256，10 万次迭代 + 随机盐，**不存明文** |
| 会话有效期内置 30 天 | 过期自动失效 |
| 停用账号 / 重置密码 / 改密 | 立即踢掉该账号所有已登录会话 |
| 删除自己 / 降级或停用最后一个管理员 | 会被拒绝（防止把自己锁在门外） |
| 查看者调用管理接口 | 返回 403 需要管理员权限 |

### 密钥与暴露面

| 项 | 状态 | 说明 |
|---|---|---|
| 真实密码存放位置 | ✅ 已移出 Git | 只在 `backend/appsettings.Local.json`（gitignored） |
| `AdminToken` | ✅ 已轮换 | 旧的已失效；它是**公网可利用**的（隧道一开就能用），所以最该换 |
| 后台 `admin` 登录密码 | ✅ 已轮换 | 旧的已失效 |
| PostgreSQL 监听 | ⚠️ `listen_addresses = '*'` | 端口在局域网可见，但 `pg_hba.conf` 只放行 `127.0.0.1/::1`，**外部主机认证会被拒绝**，风险低 |
| Git 历史 | ⚠️ 旧值仍在历史中 | 已通过**轮换**使其失效；无需改写历史 |

> 可选加固（非必须）：把 `postgresql.conf` 的 `listen_addresses` 改为 `'localhost'`
> 并重启 PostgreSQL 服务。注意这需要管理员权限，且会影响本机其它连库的工具。

---

## 十、故障排查

| 现象 | 可能原因 | 处理 |
|---|---|---|
| 扫码后页面打不开 | github.io 被微信拦截 | 用浏览器打开；或换客户自有域名 |
| 提交没反应/报错 | apiBase 未填或隧道已重开（地址变了） | 重设 config.js 的 apiBase 并 `npm run deploy` |
| 后端启动报错连不上库 | PostgreSQL 未启动或密码不符 | 确认 PG 服务在跑；`appsettings.Local.json` 密码与安装时一致 |
| 一键启动卡在"后端启动超时" | PostgreSQL 服务没开 / 密码不对 | 看窗口里 `[后端]` 的报错行；确认 PG 服务已启动 |
| 一键启动卡在"获取隧道地址超时" | 本机代理拦了 Cloudflare | 脚本已自动清代理；若仍失败，手动 `net stop` 代理类软件后重试 |
| 隧道地址每次变 | 用的是临时 quick tunnel | 测试无妨；正式用 Cloudflare 命名隧道 + 客户域名 |
| 登录提示"用户名或密码错误" | 忘记密码 | 用主令牌后门登录 → 账号管理里重置密码 |
| 页面显示"登录已失效" | 会话超 30 天 / 账号被停用或改密 | 重新登录即可 |
| 改完配置线上没变化 | GitHub Pages 有约 10 分钟 CDN 缓存 | 等 10 分钟；或加 `?t=时间戳` 强制刷新 |
| 查看者点不到 LOGO/账号管理 | 这是设计如此（权限隔离） | 需要改配置就用管理员账号登录 |
| 发布时构建报 `SAFE_DELETE` | 受管环境拦截了 vite 清空 `dist` | 脚本会自动对该次构建关闭删除保护重试一次；若仍失败，手动在 `frontend/` 下跑 `npm run build` |
